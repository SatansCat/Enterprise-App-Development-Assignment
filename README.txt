To run the server:
	ensure there is nothing else on the machine running on port 9090,
	ensure mongo is running beforehand,
	navigate to the "Individual Project" folder in a command prompt window
	execute the command "node server.js"

To view as a client:
	enter the URL "http://localhost:9090/" into a browser.


References:
	Bootstrap was utilised in the creation of the page layouts.